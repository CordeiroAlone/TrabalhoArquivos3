#ifndef DELETE_H
#define DELETE_H
    // A função presente recebe o nome do arquivo e a quantidade de vezes que deve ocorrer durante a funcionalidade da deleção e realiza o necessário para que a funcionalidade seja feita.
    #include "fornecidas.h"
    #include "util.h"
    
    int DELETE(char *arquivoBIN, int n);

#endif